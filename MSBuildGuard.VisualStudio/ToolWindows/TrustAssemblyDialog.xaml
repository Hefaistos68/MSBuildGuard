<Window x:Class="MSBuildGuard.VisualStudio.ToolWindows.TrustAssemblyDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Title="Trust Assembly"
             SizeToContent="WidthAndHeight"
             ResizeMode="NoResize"
             WindowStartupLocation="CenterOwner"
             Loaded="Window_Loaded">
    <StackPanel Margin="16">
        <TextBlock Text="Trust Assembly" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Text="You are about to trust the following assembly. All findings originating from this assembly will be automatically approved." TextWrapping="Wrap" Margin="0,0,0,12"/>

        <Border Background="#F5F5F5" Padding="12" Margin="0,0,0,16" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="AssemblyNameTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="AssemblyVersionTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Path:" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="AssemblyPathTextBlock" Text="" TextWrapping="Wrap" FontSize="11"/>
            </Grid>
        </Border>

        <TextBlock Text="Reason (optional):" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBox x:Name="ReasonTextBox" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Padding="8"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
            <Button x:Name="TrustButton" Content="Trust Assembly" Padding="16,8" Background="#2ECC71" Foreground="White" Margin="0,0,8,0" Click="TrustButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="16,8" Margin="0,0,0,0" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
