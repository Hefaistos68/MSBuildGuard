<platformUi:DialogWindow x:Class="MSBuildGuard.VisualStudio.ToolWindows.BuildBlockDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
        xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
        Title="MSBuild Guard — Build Blocked"
        Width="720"
        Height="480"
        MinWidth="480"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
        Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
        platformUi:ThemedDialogStyleLoader.UseDefaultThemedDialogStyles="True">
    <platformUi:DialogWindow.Resources>
        <ResourceDictionary>
            <Style x:Key="RiskIndicatorTextStyle" TargetType="TextBlock">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RiskLevel}" Value="High">
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUi:EnvironmentColors.VizSurfaceRedMediumBrushKey}}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding RiskLevel}" Value="Medium">
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUi:EnvironmentColors.VizSurfaceGoldMediumBrushKey}}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding RiskLevel}" Value="Low">
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static platformUi:EnvironmentColors.VizSurfaceGreenMediumBrushKey}}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <!-- Theming standard controls for VS DialogWindows -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.PanelBorderKey}}"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.PanelBorderKey}}"/>
				<Setter Property="VerticalGridLinesBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.PanelBorderKey}}"/>
                <Setter Property="RowBackground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.GridHeadingBackgroundKey}}"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.GridHeadingTextKey}}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static vsshell:VsBrushes.PanelBorderKey}}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="Padding" Value="6,4"/>
            </Style>
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>
            </Style>
            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}"/>
                        <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </platformUi:DialogWindow.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="⛔"
                       FontSize="34"
                       Foreground="{DynamicResource {x:Static platformUi:EnvironmentColors.VizSurfaceRedMediumBrushKey}}"
                       Margin="0,0,12,0"
                       VerticalAlignment="Top" />

            <StackPanel Orientation="Vertical">
                <TextBlock Text="Build Blocked by Security Policy"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" />
                <TextBlock Text="The current security scan requires action before the build can proceed."
                           Margin="0,4,0,0"
                           Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}" />
            </StackPanel>
        </StackPanel>

        <!-- Summary -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Target:" FontWeight="Bold" Width="110" Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" />
            <TextBlock Grid.Column="1"
                       Text="{Binding TargetPathDisplay}"
                       ToolTip="{Binding TargetPathDisplay}"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                       Grid.ColumnSpan="3" />

            <TextBlock Grid.Row="1" Text="Risk Score:" FontWeight="Bold" Width="110" Margin="0,4,0,0" Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}" />
            <TextBlock Grid.Row="1" Grid.Column="1"
                       Text="{Binding RiskScore}"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                       Margin="0,4,0,0" />
            <TextBlock Grid.Row="1" Grid.Column="2"
                       Text="Action Required:"
                       FontWeight="Bold"
                       Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
                       Margin="24,4,8,0" />
            <TextBlock Grid.Row="1" Grid.Column="3"
                       Text="{Binding RecommendedAction}"
                       Style="{StaticResource RiskIndicatorTextStyle}"
                       FontWeight="SemiBold"
                       Margin="0,4,0,0" />
        </Grid>

        <!-- Findings grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Findings}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.PanelBorderKey}}"
                  Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
                  Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Rule"     Binding="{Binding RuleId}"   Width="90" />
                <DataGridTextColumn Header="Title"    Binding="{Binding Title}"    Width="*" />
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80" />
                <DataGridTextColumn Header="Action"   Binding="{Binding Action}"   Width="110" />
                <DataGridTextColumn Header="File"     Binding="{Binding FilePath}" Width="160" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Proceed Anyway"
                    Width="130"
                    Margin="0,0,10,0"
                    Click="OnProceedClick" />
            <Button Content="Cancel Build"
                    Width="110"
                    IsDefault="True"
                    IsCancel="True"
                    Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</platformUi:DialogWindow>
