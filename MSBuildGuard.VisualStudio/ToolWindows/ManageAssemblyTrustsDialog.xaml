<Window x:Class="MSBuildGuard.VisualStudio.ToolWindows.ManageAssemblyTrustsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Title="Manage Assembly Trusts"
             Width="600"
             Height="400"
             WindowStartupLocation="CenterOwner"
             Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Manage Assembly Trusts" FontSize="16" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Trusted assemblies have all findings automatically approved. Trusts are pinned to specific versions." TextWrapping="Wrap" Margin="0,0,0,12" Foreground="#666666"/>

        <DataGrid Grid.Row="2" x:Name="TrustedAssembliesGrid" AutoGenerateColumns="False" Margin="0,0,0,12" CanUserSortColumns="False" CanUserReorderColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Assembly Name" Binding="{Binding Name}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="AddButton" Content="Add..." Padding="16,8" Margin="0,0,8,0" Click="AddButton_Click"/>
            <Button x:Name="RemoveButton" Content="Remove" Padding="16,8" Margin="0,0,0,0" Click="RemoveButton_Click" IsEnabled="False"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Padding="16,8" Background="#2ECC71" Foreground="White" Margin="0,0,8,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="16,8" Margin="0,0,0,0" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
