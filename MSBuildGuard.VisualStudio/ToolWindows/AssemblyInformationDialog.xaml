<Window x:Class="MSBuildGuard.VisualStudio.ToolWindows.AssemblyInformationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Title="Assembly Information"
             SizeToContent="WidthAndHeight"
             MinWidth="480"
             ResizeMode="NoResize"
             WindowStartupLocation="CenterOwner"
             Loaded="Window_Loaded">
    <StackPanel Margin="16">
        <TextBlock Text="Assembly Information" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

        <Border Background="#F5F5F5" Padding="12" Margin="0,0,0,16" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="AssemblyNameTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="AssemblyVersionTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Path:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="AssemblyPathTextBlock" Text="" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,8"/>

                <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,4,0,8"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Signer:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="AssemblySignerTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Issuer:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="5" Grid.Column="1" x:Name="AssemblyIssuerTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Subject:" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Grid.Row="6" Grid.Column="1" x:Name="AssemblySubjectTextBlock" Text="" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TrustSignerButton" Content="Trust this Signer" Padding="16,8" Margin="0,0,8,0" IsEnabled="False" Click="TrustSignerButton_Click"/>
            <Button x:Name="OpenPropertiesButton" Content="Open File Properties" Padding="16,8" Margin="0,0,8,0" Click="OpenPropertiesButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Padding="16,8" IsCancel="True" Click="CloseButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
