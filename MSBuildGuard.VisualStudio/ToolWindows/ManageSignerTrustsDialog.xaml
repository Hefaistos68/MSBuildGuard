<Window x:Class="MSBuildGuard.VisualStudio.ToolWindows.ManageSignerTrustsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Title="Manage Signer Trusts"
             Width="900"
             Height="420"
             WindowStartupLocation="CenterOwner"
             Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Manage Signer Trusts" FontSize="16" FontWeight="Bold" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,12">
            <TextBlock Text="Trusted signers have all findings from assemblies bearing their certificate automatically approved, regardless of assembly version." TextWrapping="Wrap" Foreground="#666666" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Scope:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                <ComboBox x:Name="ScopeComboBox" Width="180" SelectionChanged="ScopeComboBox_SelectionChanged"/>
            </StackPanel>
            <StackPanel x:Name="ProjectSelectorPanel" Orientation="Horizontal" Visibility="Collapsed">
                <TextBlock Text="Project:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0"/>
                <ComboBox x:Name="ProjectScopeComboBox" Width="360" DisplayMemberPath="Name" SelectedValuePath="Path" SelectionChanged="ProjectScopeComboBox_SelectionChanged"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="TrustedSignersGrid" AutoGenerateColumns="False" Margin="0,0,0,12" CanUserSortColumns="False" CanUserReorderColumns="False" CanUserAddRows="False" Loaded="TrustedSignersGrid_Loaded">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Move trust to User scope" Tag="User" Click="MoveTrustToScopeMenuItem_Click"/>
                    <MenuItem Header="Move trust to Solution scope" Tag="Solution" Click="MoveTrustToScopeMenuItem_Click"/>
                    <MenuItem x:Name="MoveToProjectScopeMenuItem" Header="Move trust to Project scope">
                        <MenuItem.ItemsSource>
                            <Binding ElementName="ProjectScopeComboBox" Path="ItemsSource"/>
                        </MenuItem.ItemsSource>
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding Name}"/>
                                <Setter Property="Tag" Value="{Binding Path}"/>
                                <EventSetter Event="Click" Handler="MoveTrustToProjectScopeMenuItem_Click"/>
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Signer" Binding="{Binding SignerName}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Issuer" Binding="{Binding Issuer}" Width="220" IsReadOnly="True"/>
                <DataGridTextColumn Header="Subject (DN)" Binding="{Binding SubjectDn}" Width="2*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Trusted Since" Binding="{Binding CreatedAtDisplay}" Width="140" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="RemoveButton" Content="Remove" Padding="16,8" Click="RemoveButton_Click" IsEnabled="False"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="Save" Padding="16,8" Background="#2ECC71" Foreground="White" Margin="0,0,8,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="16,8" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
