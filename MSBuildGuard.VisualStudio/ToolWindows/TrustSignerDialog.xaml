<Window x:Class="MSBuildGuard.VisualStudio.ToolWindows.TrustSignerDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Title="Trust Signer"
             SizeToContent="WidthAndHeight"
             ResizeMode="NoResize"
             WindowStartupLocation="CenterOwner"
             Loaded="Window_Loaded">
    <StackPanel Margin="16" Width="640">
        <TextBlock Text="Trust Signer" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Text="You are about to trust this signer. All assemblies signed by this certificate identity will be automatically approved." TextWrapping="Wrap" Margin="0,0,0,12"/>

        <Border Background="#F5F5F5" Padding="12" Margin="0,0,0,12" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Signer:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SignerNameTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Issuer:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="IssuerTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Subject:" FontWeight="Bold" Margin="0,0,8,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="SubjectTextBlock" Text="" Margin="0,0,0,8" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Thumbprint:" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="ThumbprintTextBlock" Text="" TextWrapping="Wrap" FontFamily="Consolas"/>
            </Grid>
        </Border>

        <TextBlock Text="Trust Scope:" FontWeight="Bold" Margin="0,0,0,8"/>
        <ComboBox x:Name="ScopeComboBox" Margin="0,0,0,12" SelectedIndex="0"/>

        <Expander Header="Advanced" IsExpanded="False" Margin="0,0,0,12">
            <StackPanel Margin="12,8,12,4">
                <CheckBox x:Name="NoExpirationCheckBox" Content="Does not expire" Margin="0,0,0,8" IsChecked="True" Checked="NoExpirationCheckBox_Changed" Unchecked="NoExpirationCheckBox_Changed"/>
                <TextBlock Text="Valid until:" FontWeight="Bold" Margin="0,0,0,6"/>
                <DatePicker x:Name="ValidUntilDatePicker" IsEnabled="False"/>
            </StackPanel>
        </Expander>

        <TextBlock Text="Reason (optional):" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBox x:Name="ReasonTextBox" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Padding="8"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TrustButton" Content="Trust Signer" Padding="16,8" Background="#2ECC71" Foreground="White" Margin="0,0,8,0" Click="TrustButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="16,8" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
